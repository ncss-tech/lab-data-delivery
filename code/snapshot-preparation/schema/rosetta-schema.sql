CREATE TABLE rosetta 
( `rosetta_key` REAL,
	`layer_key` REAL,
	`theta_r` REAL,
	`theta_s` REAL,
	`alpha` REAL,
	`npar` REAL,
	`usedmodel` REAL,
	`wlupdated` REAL,
	`Ks` REAL,
	`Ko` REAL,
	`Lpar` REAL 
)
