CREATE TABLE preparation 
( `prep_key` REAL,
	`prep_code` TEXT,
	`prep_abbrev` TEXT,
	`moisture_state` TEXT,
	`orig_size_frac` TEXT,
	`final_size_frac` TEXT,
	`prep_desc` TEXT,
	`prep_rpt_sort_order` REAL 
)
